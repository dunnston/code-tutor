{
  "id": 60,
  "trackId": 7,
  "language": "python",
  "title": "⭐ The Grand Finale",
  "subtitle": "Complete RPG System Challenge",
  "difficulty": 8,
  "estimatedTime": "120-180 minutes",
  "xpReward": 500,
  "description": "# ⭐ FINAL CHALLENGE: Complete RPG System\n\n🏆 **Build a complete, production-ready RPG system!**\n\n## Requirements\n\n### Core System\n- Character classes with inheritance (Warrior/Mage/Rogue)\n- Inventory system with item management\n- Battle system with turn-based combat\n- Experience and leveling\n\n### Data Persistence\n- Save/load game state to JSON\n- Export statistics to CSV\n- Backup system\n- Error handling for all file operations\n\n### Features\n- Create multiple characters\n- Equip items from inventory\n- Fight enemies\n- Level up and gain skills\n- Save/load progress\n- Generate battle reports\n\n## Success Criteria\n\n✅ Complete OOP hierarchy (5+ classes)\n✅ Inheritance and polymorphism\n✅ JSON save/load system\n✅ CSV data export\n✅ Exception handling\n✅ Playable game loop\n✅ All systems working together\n\n⚔️ **This is your masterpiece - show everything you've learned!**\n\n*Congratulations on completing Python Applications (Intermediate)!*",
  "starterCode": "# Final RPG System Challenge\n# Build a complete RPG game with all features!\n\nimport json\nimport csv\nimport io\nfrom abc import ABC, abstractmethod\n\n# TODO: Build your complete RPG system here!\n# Requirements:\n# 1. Character classes with inheritance\n# 2. Inventory and item system\n# 3. Battle system\n# 4. Save/load with JSON\n# 5. Export stats to CSV\n# 6. Error handling\n\nprint(\"=== RPG System ===\")\nprint()\nprint(\"Build your complete RPG system!\")\nprint(\"Include: OOP, JSON, CSV, Exceptions, Inheritance\")\nprint()\nprint(\"Good luck, Developer!\")",
  "solutionCode": "import json\nimport csv\nimport io\nfrom abc import ABC, abstractmethod\n\nclass Character(ABC):\n    def __init__(self, name, health, attack):\n        self.name = name\n        self.health = health\n        self.max_health = health\n        self.attack = attack\n        self.level = 1\n        self.experience = 0\n    \n    @abstractmethod\n    def special_ability(self):\n        pass\n    \n    def take_damage(self, amount):\n        self.health -= amount\n        if self.health < 0:\n            self.health = 0\n    \n    def is_alive(self):\n        return self.health > 0\n    \n    def gain_xp(self, amount):\n        self.experience += amount\n        if self.experience >= 100:\n            self.level_up()\n    \n    def level_up(self):\n        self.level += 1\n        self.experience = 0\n        self.max_health += 10\n        self.health = self.max_health\n        self.attack += 5\n        print(f\"{self.name} leveled up to {self.level}!\")\n    \n    def __str__(self):\n        return f\"{self.name} Lv{self.level} ({self.health}/{self.max_health} HP)\"\n\nclass Warrior(Character):\n    def __init__(self, name):\n        super().__init__(name, 150, 25)\n    \n    def special_ability(self):\n        return self.attack * 2\n\nclass Mage(Character):\n    def __init__(self, name):\n        super().__init__(name, 70, 35)\n    \n    def special_ability(self):\n        return self.attack * 1.5\n\nclass GameManager:\n    def __init__(self):\n        self.characters = []\n    \n    def add_character(self, character):\n        self.characters.append(character)\n    \n    def save_game(self):\n        data = []\n        for char in self.characters:\n            data.append({\n                'name': char.name,\n                'type': char.__class__.__name__,\n                'level': char.level,\n                'health': char.health,\n                'experience': char.experience\n            })\n        return json.dumps(data, indent=2)\n    \n    def export_stats(self):\n        output = io.StringIO()\n        writer = csv.DictWriter(output, fieldnames=['name', 'level', 'health'])\n        writer.writeheader()\n        for char in self.characters:\n            writer.writerow({'name': char.name, 'level': char.level, 'health': char.health})\n        return output.getvalue()\n\nprint(\"=== Complete RPG System ===\")\nprint()\n\ngame = GameManager()\ngame.add_character(Warrior(\"Ragnar\"))\ngame.add_character(Mage(\"Gandalf\"))\n\nprint(\"Characters:\")\nfor char in game.characters:\n    print(f\"  {char}\")\n\nprint(\"\\nSaving game...\")\nprint(game.save_game())\n\nprint(\"\\nExporting stats...\")\nprint(game.export_stats())\n\nprint(\"\\n🎉 Congratulations! You've completed Python Applications!\")",
  "hints": [
    "Start with Character base class using ABC",
    "Create Warrior/Mage/Rogue subclasses",
    "Build GameManager to coordinate everything",
    "Add save_game() using JSON",
    "Add export_stats() using CSV",
    "Test each component separately first"
  ],
  "validationTests": [
    {"type": "code_contains", "value": "class Character", "description": "Must create Character class"},
    {"type": "code_contains", "value": "ABC", "description": "Should use abstract base class"},
    {"type": "code_contains", "value": "json.", "description": "Must use JSON for save/load"},
    {"type": "code_contains", "value": "csv.", "description": "Must use CSV for exports"}
  ],
  "learningObjectives": [
    "Integrate all intermediate Python concepts",
    "Design complete application architecture",
    "Implement production-ready features",
    "Build maintainable, extensible systems",
    "Master intermediate Python development"
  ],
  "previousLessonId": 59,
  "tags": ["intermediate", "final-challenge", "oop", "json", "csv", "complete-system"],
  "commonMistakes": [
    {"mistake": "Not planning before coding", "explanation": "Design your class hierarchy and data flow FIRST, then implement!"},
    {"mistake": "Forgetting error handling", "explanation": "Add try/except blocks for file operations and user input."},
    {"mistake": "Not testing incrementally", "explanation": "Build and test one feature at a time, don't try to build everything at once!"}
  ],
  "bonus": {
    "description": "Add a GUI using tkinter, implement multiplayer with networking, or create a dungeon generation system!",
    "xpReward": 500
  }
}
