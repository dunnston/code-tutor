{
  "id": 59,
  "trackId": 7,
  "language": "python",
  "title": "The Character Manager",
  "subtitle": "Build a Complete Application",
  "difficulty": 6,
  "estimatedTime": "60 minutes",
  "xpReward": 300,
  "description": "# Build a Character Manager Application\n\n🎮 Combine everything: OOP, JSON, CSV, Exceptions!\n\n## Features\n\n1. Character class with full RPG stats\n2. Save/load characters to JSON\n3. Export party roster to CSV\n4. Exception handling for file operations\n5. Interactive menu system\n\n⚔️ Build a complete app!",
  "starterCode": "import json\nimport csv\nimport io\n\nclass Character:\n    def __init__(self, name, char_class, level, health, experience):\n        self.name = name\n        self.char_class = char_class\n        self.level = level\n        self.health = health\n        self.experience = experience\n    \n    def to_dict(self):\n        return {\n            'name': self.name,\n            'char_class': self.char_class,\n            'level': self.level,\n            'health': self.health,\n            'experience': self.experience\n        }\n    \n    def __str__(self):\n        return f\"{self.name} ({self.char_class}) Lv{self.level}\"\n\nclass CharacterManager:\n    def __init__(self):\n        self.characters = []\n    \n    def add_character(self, character):\n        self.characters.append(character)\n        print(f'Added {character.name}')\n    \n    def list_characters(self):\n        if not self.characters:\n            print('No characters yet')\n            return\n        for i, char in enumerate(self.characters, 1):\n            print(f'{i}. {char}')\n    \n    def save_to_json(self):\n        data = [char.to_dict() for char in self.characters]\n        json_str = json.dumps(data, indent=2)\n        print('Saved to JSON!')\n        return json_str\n    \n    def export_to_csv(self):\n        if not self.characters:\n            return ''\n        output = io.StringIO()\n        fieldnames = ['name', 'char_class', 'level', 'health', 'experience']\n        writer = csv.DictWriter(output, fieldnames=fieldnames)\n        writer.writeheader()\n        for char in self.characters:\n            writer.writerow(char.to_dict())\n        return output.getvalue()\n\n# Demo\nmanager = CharacterManager()\nmanager.add_character(Character('Warrior', 'warrior', 10, 150, 890))\nmanager.add_character(Character('Mage', 'mage', 12, 70, 1100))\n\nprint('\\n--- Party Roster ---')\nmanager.list_characters()\n\nprint('\\n--- JSON Export ---')\njson_data = manager.save_to_json()\nprint(json_data)\n\nprint('\\n--- CSV Export ---')\ncsv_data = manager.export_to_csv()\nprint(csv_data)",
  "solutionCode": "import json\nimport csv\nimport io\n\nclass Character:\n    def __init__(self, name, char_class, level, health, experience):\n        self.name = name\n        self.char_class = char_class\n        self.level = level\n        self.health = health\n        self.experience = experience\n    \n    def to_dict(self):\n        return {\n            'name': self.name,\n            'char_class': self.char_class,\n            'level': self.level,\n            'health': self.health,\n            'experience': self.experience\n        }\n    \n    def __str__(self):\n        return f\"{self.name} ({self.char_class}) Lv{self.level}\"\n\nclass CharacterManager:\n    def __init__(self):\n        self.characters = []\n    \n    def add_character(self, character):\n        self.characters.append(character)\n        print(f'Added {character.name}')\n    \n    def list_characters(self):\n        if not self.characters:\n            print('No characters yet')\n            return\n        for i, char in enumerate(self.characters, 1):\n            print(f'{i}. {char}')\n    \n    def save_to_json(self):\n        data = [char.to_dict() for char in self.characters]\n        json_str = json.dumps(data, indent=2)\n        print('Saved to JSON!')\n        return json_str\n    \n    def export_to_csv(self):\n        if not self.characters:\n            return ''\n        output = io.StringIO()\n        fieldnames = ['name', 'char_class', 'level', 'health', 'experience']\n        writer = csv.DictWriter(output, fieldnames=fieldnames)\n        writer.writeheader()\n        for char in self.characters:\n            writer.writerow(char.to_dict())\n        return output.getvalue()\n\nmanager = CharacterManager()\nmanager.add_character(Character('Warrior', 'warrior', 10, 150, 890))\nmanager.add_character(Character('Mage', 'mage', 12, 70, 1100))\n\nprint('\\n--- Party Roster ---')\nmanager.list_characters()\n\nprint('\\n--- JSON Export ---')\njson_data = manager.save_to_json()\nprint(json_data)\n\nprint('\\n--- CSV Export ---')\ncsv_data = manager.export_to_csv()\nprint(csv_data)",
  "hints": ["Combine OOP with file operations", "Use to_dict() for serialization", "Manager class coordinates everything"],
  "validationTests": [
    {"type": "code_contains", "value": "class CharacterManager", "description": "Must create manager class"},
    {"type": "code_contains", "value": "json.dumps", "description": "Must use JSON"},
    {"type": "code_contains", "value": "csv.DictWriter", "description": "Must use CSV"},
    {"type": "output_contains", "value": "Warrior", "description": "Should manage characters"}
  ],
  "learningObjectives": ["Build complete applications", "Integrate multiple concepts", "Design class systems", "Handle data persistence"],
  "nextLessonId": 60,
  "previousLessonId": 58,
  "tags": ["intermediate", "application", "oop", "json", "csv", "integration"]
}
